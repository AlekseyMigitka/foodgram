# Foodgram

Foodgram — это веб-приложение для хранения, поиска и публикации рецептов с возможностью делиться ссылками, сохранять любимые рецепты и просматривать инструкции.

---

## 🎯 Возможности проекта

- Просмотр и поиск рецептов
- Создание, редактирование и удаление своих рецептов
- Управление избранными рецептами
- Добавление рецептов в список покупок
- Пользовательская регистрация и аутентификация
- Документация API через ReDoc
- Адаптивный фронтенд

---

## 🛠 Использованные технологии

**Бэкенд:**
- Python 3.12
- Django 5.1
- Django REST Framework
- PostgreSQL
- Djoser (авторизация и регистрация)
- Gunicorn (WSGI сервер)

**Фронтенд:**
- React 17
- JavaScript, HTML, CSS
- npm, Webpack

**Инфраструктура и деплой:**
- Docker, Docker Compose
- Nginx
- GitHub Actions (CI/CD)
- DuckDNS (домен)

---

## 🚀 Локальный запуск проекта

1. Клонируем репозиторий:

```bash
git clone <репозиторий>
cd foodgram
```

2. Создаём `.env` файл:

```env
POSTGRES_DB=foodgram
POSTGRES_USER=foodgram_user
POSTGRES_PASSWORD=foodgram_password
DB_HOST=db
DB_PORT=5432
SECRET_KEY=<ваш_secret_key>
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost
```

3. Собираем и запускаем контейнеры:

```bash
docker compose up -d --build
```

4. Выполняем миграции и собираем статику:

```bash
docker compose exec backend python manage.py migrate
docker compose exec backend python manage.py collectstatic --noinput
```

5. Открываем сайт: [http://127.0.0.1:9000](http://127.0.0.1:9000)

---

## 🌐 Деплой на сервер

1. Копируем `docker-compose.yml` и `.env` на сервер
2. Запускаем контейнеры на сервере:

```bash
docker compose up -d --build
```

7. Проверяем сайт: [http://recipetop.duckdns.org](http://recipetop.duckdns.org)

---

## ⚙ Переменные окружения

- `POSTGRES_DB` — имя базы данных
- `POSTGRES_USER` — пользователь БД
- `POSTGRES_PASSWORD` — пароль пользователя БД
- `DB_HOST` — хост PostgreSQL
- `DB_PORT` — порт PostgreSQL
- `SECRET_KEY` — секретный ключ Django
- `DEBUG` — включение режима отладки (`True`/`False`)
- `ALLOWED_HOSTS` — список разрешённых хостов, через запятую

---

## ✅ Чек-лист перед деплоем

- [ ] Все миграции выполнены
- [ ] Статика собрана
- [ ] Контейнеры Docker собраны и работают
- [ ] HTTPS сертификат установлен
- [ ] Домен прописан в `ALLOWED_HOSTS`
- [ ] CI/CD workflow настроен

---

## 👤 Автор проекта

Алексей Мигитка  
- GitHub: [https://github.com/AlekseyMigitka](https://github.com/AlekseyMigitka)  
- DockerHub: [https://hub.docker.com/u/alekseymigitka](https://hub.docker.com/u/alekseymigitka)

---

## 🔗 Ссылка на сайт

[http://recipetop.duckdns.org](http://recipetop.duckdns.org)

